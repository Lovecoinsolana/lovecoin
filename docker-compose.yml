version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: lovecoin-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: lovecoin
      POSTGRES_PASSWORD: lovecoin
      POSTGRES_DB: lovecoin
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lovecoin -d lovecoin"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
